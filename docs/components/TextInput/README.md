# TextInput

Text input with cursor navigation, paste highlighting, password masking, and Enter submission. Controlled (`TextInput`) and uncontrolled (`UncontrolledTextInput`) variants with storage persistence. Supports single-line virtual-scrolling for long text and multi-line wrap mode with vertical cursor navigation.
