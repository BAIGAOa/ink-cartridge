# Storage System

## Why

Terminal apps need persistent state — settings, scores, preferences. The storage system provides typed async key-value persistence backed by a single JSON file on disk.

## API Index

| API | Purpose |
|-----|---------|
| [createStorage](./createStorage-API.md) | Create a typed JSON key-value store |
